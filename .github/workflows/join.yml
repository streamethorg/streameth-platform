name: Join videos

on:
  workflow_dispatch:
  push:
    paths:
      - 'assets/splits/**.mp4'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v3
        with:
          node-version: 'latest'
      - run: sudo apt install -y nscd
      - run: yarn install
      - run: yarn join
      - uses: EndBug/add-and-commit@v9
        with:
          default_author: github_actions
          message: '[av] join videos'
